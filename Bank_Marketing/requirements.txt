# python -m pip install -r requirements.txt
# run the above command in the cmd prompt to install all packages.
# required libraries:

pandas >= 1.1.2
numpy >= 1.19.1
phik >= 0.11.0
matplotlib >= 3.3.1
seaborn >= 0.11.1
statsmodels >= 0.12.1
optuna >= 2.5.0
scikit-learn >= 0.23.2
imbalanced-learn >= 0.7.0
